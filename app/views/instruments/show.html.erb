<div class="container">

  <h2>Instrument</h2>

  <div class="show-flex">

    <div class="card-product">
      <%= cl_image_tag @instrument.photo, brightness: 30, radius: 20, width: 300, height: 300, crop: :thumb %>
        <div class="card-product-infos">
          <h4><%= @instrument.name.capitalize %></h4>
          <h6><%= @instrument.category %></h6>
            <p>
              <%= @instrument.description %>
            </p>
          <h5>
            City: <%= @instrument.city %>
          </h5>
          <h5>
            Price per day: <%= @instrument.price %>€
          </h5>
        <button type="btn btn-success">Booking button</button>
      </div>
    </div>
  </div>
</div>

  <div class="show-booking">
  </div>
</div>
